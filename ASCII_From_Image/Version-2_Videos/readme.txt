
OPTIONS TO TEST
--width 120         # sets how wide the ASCII art is (in characters)
--scale             # Something I don't quite get, but this and the width are things you need to tamper with
--mode hdr          # uses full 24-bit ANSI color, the most vivid mode
--mode 3-gradient   # RGB “color bands” style (fastest)
--mode grayscale    # monochrome
--invert            # flip brightness → good for bright-on-dark terminals
--fps 24            # cap playback at 24 frames/sec (default 30)
--no-hud            # Disables hud

PLOTTER
--window 20         # For many frame videos


pip install pandas matplotlib
pip install sounddevice soundfile

